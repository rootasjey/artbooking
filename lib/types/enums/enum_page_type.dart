/// Modular page types.
enum EnumPageType {
  /// Home page created by staff.
  home,

  /// A page to show an user's profile.
  /// This page can contain user's profile picture, they last llustrations,
  /// books, in any order.
  profile,
}
