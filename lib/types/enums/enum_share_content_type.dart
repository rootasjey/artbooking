/// Content type to share.
enum EnumShareContentType {
  /// The content being shared is a book (as a link or it's image cover).
  book,

  /// The content being shared is an illustration (as a link or it's thumbnail).
  illustration,
}
