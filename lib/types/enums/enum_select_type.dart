/// Define how to handle selected content compared to existing content.
enum EnumSelectType {
  /// Selected content will be added to existing one.
  add,

  /// Selected content will replace existing one.
  replace,
}
