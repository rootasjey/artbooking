enum EnumBookLayoutOrientation {
  /// The layout can be scrolled horizontally and vertically.
  both,

  /// The layout can only be scrolled horizontally.
  horizontal,

  /// The layout can only be scrolled vertically.
  vertical,
}
