enum DiscoverType {
  authors,
  references,
}

enum ItemsLayout {
  list,
  grid,
}

enum ItemComponentType {
  card,
  row,
}

enum SnackType {
  error,
  info,
  success,
}

enum ImageVisibility {
  acl,
  challenge,
  contest,
  gallery,
  private,
  public,
}
