import 'package:flutter/material.dart';

/// When attempting to validate an action.
class EnterIntent extends Intent {
  const EnterIntent();
}

/// When attempting to cancel an action.
class EscapeIntent extends Intent {
  const EscapeIntent();
}
